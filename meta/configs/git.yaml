- link:
    ~/.gitconfig: git/gitconfig
    ~/.gitignore.global: git/gitignore.global

- shell:
  -
    description: "Installing Git and Hub"
    command: "brew install git hub"
    stdout: true
    stderr: true